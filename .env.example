# Optional build-time Supabase config.
# Leave unset to run local-only, or configure it in the app under Settings →
# Realtime sync (which is what an already-installed APK has to use).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
